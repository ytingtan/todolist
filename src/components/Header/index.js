import Header from "./Header"
export default Header;

// index.js is the default -> it is the entry point for a lot of web dev stuff 
// you could do everything inside index.js (like throw Header in) but will be messy 
// purpose is just to export component 